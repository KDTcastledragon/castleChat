<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.chat.domserv.mapper.UserMapper">
  
 	<insert id="join">
	INSERT INTO users(public_id, login_id, password, nickname, friend_code)
	VALUES(#{publicId}, #{loginId}, #{password}, #{nickname}, #{friendCode})
	</insert>
	
	<select id="getUserByLoginId" resultType="com.chat.domserv.domain.UserDTO">
  	select * from users where login_id = #{loginId};
  	</select>
  	
	<select id="getUser" resultType="com.chat.domserv.domain.UserDTO">
  	select * from users where public_id = #{id};
  	</select>
  	
	<select id="searchUsers" resultType="com.chat.contract.user.domain.UserProfileResponseDTO" >
	  	SELECT public_id, nickname, friend_code, profile_img
		FROM users
		WHERE status = 'ACTIVE'
		  AND user_id != #{userId}
<!-- 		  AND user_id &lt;&gt; #{myUserId} -->
		  AND (
		      nickname LIKE CONCAT('%', #{searchWord}, '%')
		      OR friend_code LIKE CONCAT('%', UPPER(#{searchWord}), '%')
		  )
		LIMIT 20
  	</select>
  	
  	<select id="findUserIdByPublicId" resultType="java.lang.Long">
	select user_id from users where public_id =#{publicId} and status ='ACTIVE'
	</select>
	
	<select id="findUserInfoByPublicId" resultType="com.chat.contract.room.domain.ChatUserLookupDTO">
	select user_id,public_id, nickname, friend_code, profile_img from users where public_id=#{publicId}
	</select>
	
	<select id="findUserInfoByPublicIdList"
        resultType="com.chat.contract.room.domain.ChatUserLookupDTO">
    SELECT
        user_id AS userId,
        public_id AS publicId,
        nickname,
        friend_code AS friendCode,
        profile_img AS profileImg
    FROM users
    WHERE public_id IN
    <foreach collection="publicIds" item="publicId" open="(" separator="," close=")">
        #{publicId}
    </foreach>
</select>
	
    
     <update id="changePassword">
    	update user set password = #{encodedNewPassword} where id = #{id}
    </update>

	<update id="updatePasswordByUserId">
		UPDATE users
		SET password = #{encodedNewPassword}
		WHERE user_id = #{userId}
	</update>

	<update id="updateMyProfile">
		UPDATE users
		SET
			nickname = #{nickname},
			profile_img = #{profileImg}
		WHERE user_id = #{userId}
		  AND status = 'ACTIVE'
	</update>
    
    <delete id="withdrawMember">
    	delete from user where id = #{id}
    </delete>
    
  	<select id="friendList" resultType="com.chat.domserv.domain.UserDTO">
  	call get_friend_list(#{login_id});
  	</select>
  	
  	<select id="getUserLoginId" resultType="java.lang.String">
  	select login_id from users where user_id = #{userId};
  	</select>
  	
  	<select id="allUsers" resultType="com.chat.domserv.domain.UserDTO">
  	select * from users;
  	</select>
  	
  	<insert id="createRoom" useGeneratedKeys="true" keyProperty="roomId">
    INSERT INTO chat_rooms (room_type, room_status)
    VALUES ('DIRECT', 'ACTIVE')
	</insert>
	
	
  
  </mapper>
