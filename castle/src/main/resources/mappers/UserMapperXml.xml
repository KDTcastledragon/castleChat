<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iplist.ipboot.mapper.UserMapper">
	<select id="selectById" resultType="com.iplist.ipboot.domain.UserDTO">
        SELECT * FROM friend where req_id = #{userId} or fri_id = #{userId} and status = 'accept';
    </select>
</mapper>