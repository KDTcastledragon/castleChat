<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.iplist.ipboot.mapper.ChatMapper">
	<select id="friendsList" resultType="com.chat.castle.domain.UserDTO">
	select * from friend where req_id = #{user_id} or fri_id = #{user_id} and status ='accept';
	</select>

	<select id="chatlist"
		resultType="com.chat.castle.domain.ChatDTO">
		SELECT * FROM chatting where sender_id = #{user_id} and receiver_id={recId};
	</select>


</mapper>